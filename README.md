# Kinetik
Kinetik is a configuration-driven Android animation engine that uses compile-time bytecode instrumentation and a runtime engine to enable zero-touch UI animations across Views and Jetpack Compose.
